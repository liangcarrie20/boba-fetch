<div class="container">
  <% @shops.each do |shop| %>
  <ul>
    <li><a href="/shops/<%=shop.id%>"><%=shop.name%></a> <%=shop.address%> <%=shop.city%>, <%=shop.state%> <%=shop.zip%> </li>
  </ul>
  <% end %>
  <% if current_user %>
  <div id="add-shop"><a href="/shops/new">Add a new shop</a></div>
  <% else %>
  <p>Log in to add shops, drinks, and reviews!</p>
  <% end %>
</div>